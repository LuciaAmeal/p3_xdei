[pytest]
addopts = -q
markers =
    integration: mark slow integration tests that require external services (Orion, QuantumLeap, CrateDB)
    e2e: mark end-to-end tests that require docker-compose stack
